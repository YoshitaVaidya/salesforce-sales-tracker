<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lock_Closed_Lost_leads_from_editing</fullName>
    <active>true</active>
    <description>Currently allows reopening of Closed Lost. Can be restricted using PRIORVALUE if required.</description>
    <errorConditionFormula>ISPICKVAL( Status__c , &quot;Closed Lost&quot;)  &amp;&amp;   NOT(ISCHANGED( Status__c ))</errorConditionFormula>
    <errorMessage>Closed Lost leads cannot be edited</errorMessage>
</ValidationRule>
